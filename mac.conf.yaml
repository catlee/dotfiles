- defaults:
    link:
      relink: true

- link:
    ~/.config/kitty/kitty.conf: mac/kitty.conf
    ~/.config/aerospace/aerospace.toml: mac/aerospace.toml

- shell:
    - brew install fzf fd nvim tmux alt-tab git hyperfine ripgrep kitty bash reattach-to-user-namespace cmake pyenv aerospace
