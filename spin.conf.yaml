---
- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.tmux/tmux.conf: shell/tmux.conf
    ~/.tmux.conf: shell/tmux.conf.spin
    ~/.config/github-copilot/hosts.json: /etc/spin/secrets/copilot.json
