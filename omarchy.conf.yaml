---
- defaults:
    link:
      relink: true
      create: true
      force: true

- link:
    ~/.config/hypr: linux/omarchy/hypr
    ~/.local/bin/omarchy-adjust-window: linux/omarchy/adjust-window.sh
    ~/.gitconfig.local: gitconfig-personal


- shell:
  - sudo pacman --noconfirm -S zsh jq npm git-delta
