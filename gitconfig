[user]
        name = Chris AtLee
        email = catlee@mozilla.com

[color]
        diff = auto

[core]
        excludesfile = ~/.gitignore

[diff]
        renames = true

[alias]
        di = diff -U8 --patch-with-stat
        st = status -uno
[push]
        default = simple

[include]
        path = ~/.gitsecrets
